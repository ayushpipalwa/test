// Getting the Paragraph from HTML
var floating_text = document.getElementById("slider-text");
var given_text;

// Enter or Change your text here
given_text = "Trusted tax, legal, compliance and business advisory — all in one place.";

// Pushing Given Text to HTML
floating_text.innerHTML = given_text;
